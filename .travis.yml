language: r
cache: packages
warnings_are_errors: true
sudo: required

notifications:
  email:
    on_success: change
    on_failure: change

apt_packages:
  - r-cran-httr
  - libraptor2-dev
  - librasqal3-dev
  - librdf0
  - librdf0-dev

r_packages:
  - rmarkdown
