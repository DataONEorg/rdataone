dist: bionic
language: r
cache: packages
warnings_are_errors: true
sudo: false

notifications:
  email:
    on_success: change
    on_failure: change

addons:
  apt:
    packages:
      - librdf0
      - librdf0-dev
